name = "mixed-app"

[env.production]
server = "deploy@example.com"

[processes.web]
port = 3000
health = "/health"
resources = { memory = "256m", cpus = 0.5 }

[routes.docs]
host = "mixed.example.com"
path = "/docs"
serve = "docs-dist"

[routes.app]
host = "mixed.example.com"
process = "web"
